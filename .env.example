# Optional. In development the API host is derived from the Expo dev server, so this is usually not needed.
# Set it for physical devices on another network, staging or production.
# Android emulator -> http://10.0.2.2:5001/api
EXPO_PUBLIC_API_URL=http://192.168.1.10:5001/api
